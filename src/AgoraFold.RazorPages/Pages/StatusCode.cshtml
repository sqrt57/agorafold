@page "{id:int}"
@model StatusCodeModel
@{
    ViewData["Title"] = Model.Title;
}

<div class="text-center">
    <h1 class="display-4">@Model.Code</h1>
    <h2>@Model.Title</h2>
    <p>@Model.Message</p>
    <a asp-page="/Index">Back to browsing</a>
</div>
