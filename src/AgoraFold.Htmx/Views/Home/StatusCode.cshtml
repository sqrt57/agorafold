@model StatusCodeViewModel
@{
    ViewData["Title"] = Model.Title;
}

<div class="text-center">
    <h1 class="display-4">@Model.Code</h1>
    <h2>@Model.Title</h2>
    <p>@Model.Message</p>
    <a asp-controller="Listings" asp-action="Index">Back to browsing</a>
</div>
