services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: agorafold
      POSTGRES_USER: agorafold
      POSTGRES_PASSWORD: agorafold
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
